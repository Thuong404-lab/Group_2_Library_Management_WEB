# Cau hinh chung (an toan de commit)
spring.profiles.active=dev
spring.application.name=library-management

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.SQLServerDialect
spring.jpa.hibernate.naming.physical-strategy=com.lms.config.DatabaseNamingStrategy

# Thymeleaf
spring.thymeleaf.cache=false
spring.thymeleaf.encoding=UTF-8

# Upload file size
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Server
server.port=8080
server.servlet.context-path=/
app.base-url=http://localhost:${server.port}

# Session timeout
server.servlet.session.timeout=30m

# Cấu hình Spring Mail cho Gmail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=phamkienquoc220@gmail.com
spring.mail.password=hgsr ewxx hsfd dwsb
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
